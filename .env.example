# Server-side Anthropic API key (used by /api/anthropic Vercel serverless function).
# Set in Vercel dashboard - Project - Settings - Environment Variables (Production + Preview).
# Must NOT be exposed to the client; this is why we proxy via /api/anthropic.
ANTHROPIC_API_KEY=
